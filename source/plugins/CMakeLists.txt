if(WIN32)
	add_subdirectory(d3d11)
	add_subdirectory(opengl)
	add_subdirectory(empty)
endif()

if(UNIX)
	if(APPLE)
		add_subdirectory(metal)
	else()
		add_subdirectory(opengl)
		add_subdirectory(empty)
	endif()
endif()
