if(WIN32)
	add_subdirectory(example_app)
	add_subdirectory(d3d11_app)
	add_subdirectory(opengl_app)
endif(WIN32)
	
if(UNIX)
	if(APPLE)
		add_subdirectory(macos_app)
	else()
		add_subdirectory(opengl_app)
	endif(APPLE)
endif(UNIX)
